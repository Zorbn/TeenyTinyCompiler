function printThree(a: float, b: float, c: float): void do
    printTwo(a, b)
    print c
endfunction

function printTwo(a: float, b: float): void do
    print a
    print b
endfunction

function max(a:float, b:float):float do
    if a > b then
        return a
    endif

    return b
endfunction

printThree(1.5, 2.0 + 3.0, 4.0 / 0.33)
print max(1.0, 2.0)